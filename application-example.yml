spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tablemate
    username: postgres
    password: your_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
